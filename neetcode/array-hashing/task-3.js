// Две суммы
// Дан массив целых чисел numsи целое число target, вернуть индексы iи jтакие, что nums[i] + nums[j] == targetи i != j.
//
// Можно предположить, что каждый вход имеет ровно одну пару индексов iи jудовлетворяет условию.
//
// Верните ответ, указав сначала меньший индекс.
//
// Пример 1:
//
// Input: nums = [3,4,5,6], target = 7
//
// Output: [0,1]

// Решение

class Solution {
  /**
   * @param {number[]} nums
   * @param {number} target
   * @return {number[]}
   */
  twoSum(nums, target) {

  }
}

const solution = new Solution()

console.log(solution.twoSum([3,4,5,6], 7))
console.log(solution.twoSum([4,5,6], 10))
console.log(solution.twoSum([5,5], 10))
